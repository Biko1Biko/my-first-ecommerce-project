Git =  helps us tracks changes in our code
Repository = a folder when git is tracking chnanges

.gitignore = tells git which files and folders to ingoner (not to track changes)

Routing = create multiples pages in React
Routing lest us create multiple pages using 1 HTML file.

React is single page application (spa)
 = we only have 1 HTML file
 = we use React to create multiple pages
